# Low-sample event partition

This non-public website partition preserves events with `postCount < 1000`.
It is excluded from `docs/live` search, listings, totals, and detail routes.
Events below 1,000 posts or outside the 360-event public release cap are retained here.
The partition contains 47 events and 30,912 posts.
