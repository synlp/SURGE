"""CMA reference probe.

Submodules:

* :mod:`benchmark.cma.dataset` — multi-bin dataset / dataloader factory
  built around precomputed BERT [CLS] embeddings.
* :mod:`benchmark.cma.blocks` — :class:`IntraBinEncoder` and
  :class:`TextAuxHead`.
* :mod:`benchmark.cma.run_cma` — :class:`CMAModel` plus the training /
  evaluation entry point.
"""
